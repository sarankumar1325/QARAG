{
  "name": "qarag-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend/qarag && bun run build",
    "vercel-build": "cd frontend/qarag && bun run build"
  }
}
